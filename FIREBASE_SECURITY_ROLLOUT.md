# Firebase Security Rollout — الدليل الشامل

الحالة الحالية: ملفات `firestore.rules` و`storage.rules` موجودة للمراجعة فقط، وليست مربوطة في `firebase.json` حتى لا يتم نشرها على الإنتاج قبل الاختبار.

## ما تم تأمينه في الكود

- منع الاعتماد على `localStorage` وحده لإظهار لوحة الإدارة.
- لوحة الإدارة لا تُفتح إلا بعد مطابقة Firebase Auth UID مع المستخدم الحالي ثم التحقق من `members/{uid}.isAdmin === true`.
- منع إنشاء حساب Admin أو إضافة `isAdmin:true` من المتصفح أثناء تسجيل دخول الأدمن.
- منع تعديل الملف الشخصي من تصفير المشاهدات والاتصالات والحفظ والتقييمات والمراجعات أو تغيير `createdAt`.
- CI يبني الويب ويفحص Syntax الخاص بـ Firebase Functions.

## مانع النشر رقم 1 — بيانات اعتماد الأدمن

دخول الأدمن الحالي ما زال يعتمد على قيم موجودة في كود العميل/إعدادات عامة:

- `cfg.adminPin` يأتي من `config/appSettings` الذي يحتاج التطبيق العادي لقراءته.
- كلمة مرور Firebase الحالية للأدمن تُشتق بشكل ثابت من رقم الهاتف داخل كود الواجهة.

لذلك لا يجب اعتبار الـ PIN أو كلمة المرور المشتقة سراً إنتاجياً.

### الترحيل المقترح

1. إنشاء طريقة دخول Admin حقيقية لا تعتمد على سر قابل للقراءة من الواجهة، مثل Firebase Auth credential مستقل أو Custom Claims يتم ضبطها من Admin SDK.
2. إزالة `adminPin` من `config/appSettings` بعد نقل الدخول.
3. عدم وجود أي كود Client يملك القدرة على تعيين `isAdmin:true`.
4. اعتبار `members/{uid}.isAdmin` أو Custom Claim مجرد صلاحية بعد تسجيل دخول قوي، وليس وسيلة تسجيل الدخول نفسها.

## مانع النشر رقم 2 — اختبار Rules

قبل ربط الملفات في `firebase.json` يجب اختبار السيناريوهات التالية باستخدام Firebase Emulator أو مشروع اختبار:

- تسجيل عضو جديد واختيار باقة وهو Pending.
- تعديل العضو لبياناته وصوره بدون تغيير الحقول الإدارية.
- البحث وفتح ملفات الأعضاء المعتمدين بدون تسجيل دخول.
- إضافة تقييم وتحديث معدل التقييم.
- حفظ عضو/إلغاء الحفظ والمتابعة/إلغاء المتابعة.
- إنشاء منشور وصورة/فيديو، Like، Comment، وحذف المحتوى المملوك.
- الرسائل بين عضوين وإنشاء إشعار للطرف الآخر.
- نشر وظيفة والتقديم عليها وزيادة عداد المتقدمين.
- إرسال طلب «اطلب صنايعي» كزائر.
- تعليم الإشعارات كمقروءة.
- دخول الأدمن وقراءة كل الأعضاء والمدفوعات والإعلانات والطلبات.
- اعتماد/رفض عضو، تغيير الباقة، التجديد، إضافة دفعة، إعدادات التطبيق والإعلانات.
- رفع avatar/cover/work photos/post media، ورفع الإعلان من الأدمن.

## تحسينات يفضل نقلها للسيرفر لاحقاً

بعض العدادات والإشعارات يتم تحديثها حالياً من المتصفح لتوافق النظام الحالي. الأفضل نقلها إلى Cloud Functions تدريجياً:

- views / saves / followers / ratings / comments / applicants counters.
- إنشاء الإشعارات الناتجة عن الرسائل والمتابعة وزيارة الملف.
- عمليات الدفع والتفعيل والتجديد الحساسة.

هذا يقلل سطح الهجوم ويجعل Firestore Rules أبسط وأشد.

## ترتيب التفعيل

1. إنهاء ترحيل تسجيل دخول الأدمن.
2. تشغيل Emulator واختبار السيناريوهات السابقة.
3. تعديل أي Rule تتعارض مع سلوك صحيح في التطبيق.
4. إضافة `firestore.rules` و`storage.rules` إلى `firebase.json`.
5. نشر القواعد فقط بعد نجاح الاختبارات.
6. مراقبة `permission-denied` بعد النشر والتأكد أن المستخدمين الطبيعيين لم تتعطل وظائفهم.
