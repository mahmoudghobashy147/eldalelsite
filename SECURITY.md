# سياسة الأمان — الدليل الشامل

## الإصدارات المدعومة

المشروع حاليًا على سلسلة الإصدار `2.x`، وهي السلسلة التي تستقبل إصلاحات الأمان والتحديثات.

| الإصدار | مدعوم |
| --- | --- |
| 2.x | ✅ نعم |
| أقدم من 2.x | ❌ لا |

## الإبلاغ عن ثغرة

لو اكتشفت مشكلة أمنية، لا تنشر تفاصيل الاستغلال أو بيانات المستخدمين في Issue عامة. تواصل مع إدارة المشروع بشكل خاص واذكر:

- الجزء المتأثر من الموقع أو التطبيق.
- خطوات إعادة ظهور المشكلة بشكل مختصر.
- التأثير المتوقع بدون مشاركة بيانات حساسة أو حقيقية لمستخدمين.

## مبادئ الأمان في المشروع

- لا يتم وضع كلمات مرور، مفاتيح حسابات الخدمة أو أسرار إدارية داخل المستودع.
- أي سر ظهر سابقًا في Git أو نسخة منشورة يُعامل كسرّ مكشوف ويجب تغييره؛ حذف النص من آخر Commit وحده لا يعيد السر إلى حالة آمنة.
- ملفات حسابات الخدمة وملفات توقيع Android (`*.jks` و`*.keystore`) يجب أن تظل خارج Git وتُخزن في GitHub Actions Secrets أو مدير أسرار مناسب.
- صلاحيات الإدارة والعمليات الحساسة يجب التحقق منها على Firebase/الخادم، وليس اعتمادًا على بيانات مخزنة محليًا في المتصفح.
- إنشاء الإشعارات، عدادات التفاعل، وطلبات الصنايعية الحساسة يتم من Cloud Functions الموثوقة بدل السماح للعميل بتعديل القيم مباشرة.
- أي تغيير في قواعد Firestore أو Storage يجب اختباره على Emulator قبل نشره على بيئة الإنتاج.
- ترتيب الإصدار الآمن: نشر Cloud Functions أولًا، ثم Firestore/Storage Rules، ثم نسخة الموقع؛ لا يتم تشديد Rules قبل وجود Functions التي تعتمد عليها الواجهة.
